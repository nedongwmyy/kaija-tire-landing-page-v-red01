"use client"

import { Button } from "@/components/ui/button"
import { useTranslation } from "@/hooks/useTranslation"

export function HeroSection() {
  const { t } = useTranslation() // language 变量在这里不再用于alt文本，所以可以省略

  const backgroundImage = "https://raw.githubusercontent.com/kaffiri-dm/piccndnow/main/p4p2509/86a7d163e9b5b70bf971f987da9d081d";

  return (
    <section 
      className="relative w-full pt-[100px] pb-[100px] flex items-center overflow-hidden 
                 bg-red-600 bg-cover bg-center bg-no-repeat" // 红色背景作为回退和底色，然后图片覆盖
      style={{ 
        backgroundImage: `url(${backgroundImage})`,
        minHeight: '650px' // 增加最小高度以更好地展示背景图和内容
      }}
    >
      {/* 渐变遮罩层，让文本更清晰，同时保留背景图可见性 */}
      <div className="absolute inset-0 bg-gradient-to-r from-black/60 via-black/40 to-transparent"></div>
      
      {/* 内容容器 */}
      {/* 
        小屏幕 (默认): flex-col, items-center, text-center (垂直居中，文本居中)
        大屏幕 (lg: 及以上): flex-row (如果需要内容区内部的左右布局), lg:justify-end (内容容器靠右), lg:items-center (垂直居中), lg:text-right (文本右对齐)
      */}
      <div 
        className="relative z-10 w-full px-4 sm:px-6 lg:px-8 text-white 
                   flex flex-col items-center text-center 
                   lg:flex-row lg:justify-end lg:items-center lg:text-right"
        style={{ minHeight: 'inherit' }} // 继承 section 的最小高度
      >
        <div className="max-w-xl w-full mt-[100px] lg:mt-0 lg:max-w-2xl"> {/* 调整最大宽度 */}
          <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black mb-4 text-balance leading-tight tracking-tight text-white">
            {t.hero.title}
          </h1>
          
          <p className="text-xl sm:text-2xl md:text-3xl mb-8 text-white/90 max-w-xl md:max-w-2xl mx-auto lg:ml-auto text-pretty leading-relaxed">
            {t.hero.subtitle}
          </p>

          {/* CTA 按钮容器 */}
          <div className="flex justify-center lg:justify-end mb-6"> {/* 小屏幕居中，大屏幕右对齐 */}
            <Button 
              size="lg" 
              className="bg-white text-red-600 hover:bg-gray-100 font-bold text-base sm:text-lg px-8 py-4 rounded-full shadow-lg transition-all duration-300 transform hover:scale-105"
              onClick={() => {
                if (typeof window !== 'undefined') {
                  window.open('https://api.whatsapp.com/send?phone=8613902337334', '_blank');
                }
              }}
            >
              {t.hero.cta}
            </Button>
          </div>
        </div>
      </div>
    </section>
  )
}




